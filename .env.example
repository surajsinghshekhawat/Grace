# Frontend (Vite) — leave empty to use same-origin /api via dev proxy
# VITE_API_URL=http://127.0.0.1:8000
VITE_API_URL=
# Shown on forgot-password screen when users need human support
# VITE_SUPPORT_EMAIL=support@example.com
