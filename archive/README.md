# Archive

Material kept for reference but **not** part of the production build.

| Folder | Contents |
|--------|----------|
| `figma-ui-prototype/` | Earlier React + TypeScript + shadcn UI tree (`app/`, `main.tsx`, styles). The live app uses `src/main.jsx` → `src/App.jsx` and `src/pages/`. Do not wire this back in without reconciling routes and APIs. |

To restore locally for comparison only, copy files back under `src/` and point `index.html` at the alternate entry (not recommended for production).
